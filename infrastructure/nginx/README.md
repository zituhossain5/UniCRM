# Nginx infrastructure

The production reverse-proxy configuration will live here when the deployment environment is
introduced. UniCRM's target routing is same-origin: `/api/*` to NestJS and all other requests to
Next.js.
