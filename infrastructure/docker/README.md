# Docker infrastructure

Local PostgreSQL and Redis services are defined in the repository root
`docker-compose.yml`. Application containers are intentionally deferred so local Next.js and
NestJS hot reload remain straightforward during foundation development.
